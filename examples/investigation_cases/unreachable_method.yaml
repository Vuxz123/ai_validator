id: unreachable_method
project:
  version: 1
  unity_root: Game
  source_groups:
    first_party:
    - Assets/Feature/**
    third_party:
    - Assets/Vendor/**
  rule_packs:
  - code_impact
execution:
  entrypoint: Scenario.Run
  assumption: Called once. This is the only external entrypoint in this closed synthetic
    program; no reflection, dynamic dispatch or other external calls occur.
baseline_files:
  Game/Assets/Feature/Subject.cs: |
    public class Scenario {
        public int Run() { return 7; }
        private int Inspect(string value) { return value == null ? 0 : value.Length; }
    }
files:
  Game/Assets/Feature/Subject.cs: |
    public class Scenario {
        public int Run() { return 7; }
        private int Inspect(string value) { return value.Length; }
    }
expected:
  status: PASS
  findings: []
